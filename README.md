# a11y-checker

Self-hosted accessibility checker using Playwright + axe-core (WCAG A/AA tags), with site crawl and exportable HTML reports.

## Run locally
npm install
npm run install:browsers
npm run dev

Open http://localhost:8080
